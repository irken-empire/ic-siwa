{
  "name": "ic-siwa-workspace",
  "type": "module",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "install-deps": "bun install && cd libs/ic_siwa_ts && bun install && bun run build && cd ../../canisters/test_canister_ts && bun install"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.0.0",
    "globals": "17.3.0",
    "typescript": "5.9.3",
    "typescript-eslint": "8.54.0"
  }
}
